name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Bump service worker cache version
        run: |
          TIMESTAMP=$(date +%s)
          sed -i "s/const CACHE = \"diet-tracker-v[^\"]*\"/const CACHE = \"diet-tracker-v$TIMESTAMP\"/" sw.js
          echo "Cache version bumped to diet-tracker-v$TIMESTAMP"

      - name: Copy files to public folder
        run: |
          mkdir -p public
          cp index.html public/index.html
          cp sw.js public/sw.js
          cp manifest.json public/manifest.json
          cp icon-192.png public/icon-192.png
          cp icon-512.png public/icon-512.png

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          PROJECT_ID: diet-tracker-app-c22f4
